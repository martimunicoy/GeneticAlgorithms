# Installation of packages and commands to run in Linux
sudo apt-get update
sudo apt-get install libbsd-dev

#Compilation in linux:
gcc -D LINUX queens_guided-GA.c -lm -lbsd -o 8qsim

#Execution ./8qsim
